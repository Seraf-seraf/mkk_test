server:
  host: 0.0.0.0
  port: 8080

mysql:
  host: mysql
  port: 3306
  database: ${MYSQL_DATABASE:-admin}
  user: ${MYSQL_USER:-admin}
  password: ${MYSQL_PASSWORD:-admin}

redis:
  host: redis
  port: 6379

metrics:
  enabled: true
  path: /metrics

migrations:
  auto: true
